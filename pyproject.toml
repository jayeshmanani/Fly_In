[project]
name = "fly-in"
version = "0.1.0"
description = "dependencies management for Fly-IN"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.1.0",
    "pygame-ce>=2.5.7",
]
